``Single source of truth for the tsl version.

Referenced by project.janet (for jpm metadata) and by the CLI
(--help and --version output). Bump this value when tagging a release.
``

(def VERSION
  ``Current tsl version string in major.minor.patch form.``
  "0.3.0")
